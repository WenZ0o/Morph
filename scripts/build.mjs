import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');let html=fs.readFileSync(path.join(root,'index.html'),'utf8');const css=fs.readFileSync(path.join(root,'styles.css'),'utf8');html=html.replace('<link rel="stylesheet" href="styles.css">',`<style>${css}</style>`).replace('<link rel="icon" href="assets/brand/morph-mark.svg" type="image/svg+xml">','');const scripts=[...html.matchAll(/<script src="([^"]+)"><\/script>/g)].map(m=>m[1]);const js=scripts.map(s=>fs.readFileSync(path.join(root,s),'utf8')).join('\n');html=html.replace(scripts.map(s=>`<script src="${s}"></script>`).join(''),`<script>${js}</script>`);fs.writeFileSync(path.join(root,'MORPH-1.html'),html);console.log(`Built MORPH-1.html (${Buffer.byteLength(html)} bytes)`);
