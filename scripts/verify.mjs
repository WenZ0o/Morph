import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';import {execFileSync} from 'node:child_process';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const required=['README.md','LICENSE','index.html','MORPH-1.html','styles.css','package.json','data/benchmarks/reference.json','docs/METHODS.md','docs/ARCHITECTURE.md'];for(const f of required)if(!fs.existsSync(path.join(root,f)))throw new Error(`Missing ${f}`);execFileSync(process.execPath,['--test','tests/*.test.js'],{cwd:root,stdio:'inherit',shell:true});console.log('Verification complete.');
