# Replay format

Exports contain `seed`, `cycle` and `events`. Fixed protocol events can be replayed deterministically. Manual brush ablations are currently recorded with counts but not geometric brush coordinates, so they are archival metadata rather than fully reconstructable interventions in version 0.1.0.
