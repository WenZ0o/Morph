# GitHub upload

Create an empty repository named `morph-1`, upload this complete folder tree, then set `main` as the default branch. The repository is ready for GitHub Pages if desired; `index.html` is the source-mode entry point and does not require a build step.
