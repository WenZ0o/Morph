# Validation

Automated checks cover deterministic repeated seeds, long-run baseline population, tissue removal followed by regrowth, signal-field orientation and starvation nutrient reduction. CI runs the tests, benchmark, portable build and package verification on Node.js 20 and 22.
